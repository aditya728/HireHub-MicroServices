> Upcoming small fixes & upgrades

1. Job entity — missing validation annotations like @NotNull @NotBlank
2. Properties split between local and config server
3. Have custom exceptions for Field validations